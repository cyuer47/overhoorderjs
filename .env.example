# Application Configuration
SECRET=your-very-long-random-secret-key-change-this-in-production
PORT=3000
NODE_ENV=development

# Security
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
UPDATE_SECRET=your-update-secret-key

# Logging
DEBUG_REQUESTS=false

# Database
DATABASE_PATH=./data.db

# HTTP base URL used when generating reset links (override in production)
BASE_URL=http://localhost:3000

# Email / SMTP settings
# To send real e-mails, set your SMTP provider details below.
# Example (SMTP provider):
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=frederick.stoltenberg51@ethereal.email
SMTP_PASS=GN3B1wCyXCEPgGMzE7
EMAIL_FROM="Overhoorder <no-reply@ivenboxem.nl>"
EMAIL_DOMAIN=ivenboxem.nl

# For development testing using Ethereal (preview links in console), set:
# USE_ETHEREAL=1

# Allow self-signed certificates for SMTP (development only, insecure):
# SMTP_ALLOW_SELF_SIGNED=1
USE_ETHEREAL=1

